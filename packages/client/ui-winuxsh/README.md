# @cmx666/dsh-client-ui-winuxsh

Adds a Winuxsh card to DSH Web Settings > Plugins. The switch stores the preference in the browser and requires a DSH restart because provider composition happens during profile startup.
